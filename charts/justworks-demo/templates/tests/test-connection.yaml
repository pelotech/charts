apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "justworks-demo.fullname" . }}-test-connection"
  labels:
    {{- include "justworks-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "justworks-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
