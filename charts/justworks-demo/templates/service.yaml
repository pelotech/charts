apiVersion: v1
kind: Service
metadata:
  name: {{ include "justworks-demo.fullname" . }}
  labels:
    {{- include "justworks-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "justworks-demo.selectorLabels" . | nindent 4 }}
